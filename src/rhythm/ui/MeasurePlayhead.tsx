import {useEffect,useRef} from 'react';
export function MeasurePlayhead({startTimeMs,durationMs,loop=false}:{startTimeMs:number;durationMs:number;loop?:boolean}){const cursor=useRef<HTMLDivElement>(null);useEffect(()=>{if(!startTimeMs){cursor.current?.style.setProperty('--playhead-progress','0');return}let frame=0;const draw=()=>{const elapsed=Math.max(0,performance.now()-startTimeMs);const progress=loop?(elapsed%durationMs)/durationMs:Math.min(1,elapsed/durationMs);if(cursor.current)cursor.current.style.setProperty('--playhead-progress',String(progress));if(loop||progress<1)frame=requestAnimationFrame(draw)};frame=requestAnimationFrame(draw);return()=>cancelAnimationFrame(frame)},[loop,startTimeMs,durationMs]);return <div ref={cursor} className="measure-playhead active" aria-hidden="true"/>}
